Let $n \in \mathbb{N}$,
and let $A_1, \dotsc, A_n \subseteq \mathbb{R}^2$ be convex sets.
Prove that if any $3$ of them have a non-empty intersection,
then $\bigcap\limits_{k = 1}^{n} {A_k} \neq \emptyset$.

What if the sets are infinitely many?

What if the sets are not all convex?
