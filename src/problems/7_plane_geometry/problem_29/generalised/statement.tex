Let $n \in \mathbb{N}$,
Let $\set{A}_{i \in I}$ be compact convex subsets of $\mathbb{R}^{d}$.
Prove that if any $d + 1$ of them have a non-empty intersection,
then $\bigcap\limits_{i \in I} {A_i} \neq \emptyset$.

Let $d \in \mathbb{N}$,
and let $A_1, \dotsc, A_n \subseteq \mathbb{R}^{d}$ be convex sets.
Prove that if any $d + 1$ of them have a non-empty intersection,
then $\bigcap\limits_{k = 1}^{n} {A_k} \neq \emptyset$.

What if the sets are not all convex?
