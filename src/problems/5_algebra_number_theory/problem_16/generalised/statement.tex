For which $n \in \mathbb{N}$ can the polynomial $x^{n} y^{n} + 1$ be factored non-trivially
\begin{itemize}
  \item in $\mathbb{Q}\sqbk{x, y}$?
  \item in $\mathbb{Q}\sqbk{x, y}$ as the product of a polynomial in $x$ and a polynomial in $y$?
  \item in $\mathbb{R}\sqbk{x, y}$?
  \item in $\mathbb{R}\sqbk{x, y}$ as the product of a polynomial in $x$ and a polynomial in $y$?
  \item in $\mathbb{C}\sqbk{x, y}$?
  \item in $\mathbb{C}\sqbk{x, y}$ as the product of a polynomial in $x$ and a polynomial in $y$?
\end{itemize}
