What is the largest cardinal $\alpha$
such that there exists a set $S \subseteq \mathbb{R}^2$ with $\card\paren{S} = \alpha$,
no three elements of which are collinear,
and such that for every $p, q \in S$ the distance $\dist{p}{q}$ is integer?
\newline (What about in higher dimension?)
