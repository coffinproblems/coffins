Let $n \in \mathbb{N}$.
Given an $n$-simplex of unit volume,
and one point on each of its sides,
cut off corners from each vertex
using the given points on the sides exiting that vertex.
Prove that the total volume of the cutoff part
is less than or equal to $\mfrac{n+1}{2^n}$.
