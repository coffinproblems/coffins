Probably
  for $k=1$ and any $n$; and
  for $n$ a power of $2$ and any $k$.
To do this, use repeated geometric mean constructions
(via the semicircle construcion, for example).

(TODO: complete the solution)
