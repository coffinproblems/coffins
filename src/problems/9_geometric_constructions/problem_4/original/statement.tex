Given a point and an angle on a plane,
construct, using only straightedge and compass,
a line through the point
that cuts the angle into a triangle of minimum perimeter. \newline
Additionally, given also a segment, construct a line through the point
that cuts the angle into a triangle whose perimeter is the length of the segment.
